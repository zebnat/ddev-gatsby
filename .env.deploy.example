# Required deploy target values
S3_BUCKET_NAME=danieldev
CLOUDFRONT_DISTRIBUTION_ID=EFIL5MU5HFHL4

# Optional AWS CLI region override
# AWS_REGION=eu-west-1

# Optional profile-based auth (recommended when available)
# AWS_PROFILE=default
